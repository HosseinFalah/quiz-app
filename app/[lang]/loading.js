const Loading = () => {
    // You can add any UI inside Loading, including a Skeleton.
    return <div className="min-h-screen bg-indigo-900 flex items-center justify-center">
        <div className="loader"></div>
    </div>
}

export default Loading;